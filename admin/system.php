<?php
declare(strict_types=1);
require_once __DIR__ . '/../includes/bootstrap.php';
$user=require_login(['admin']);
$q=trim((string)($_GET['q']??''));$actionFilter=trim((string)($_GET['action_filter']??''));$where=['1=1'];$params=[];if($q!==''){$where[]='(a.details LIKE ? OR a.ip_address LIKE ? OR u.full_name LIKE ?)';$params[]="%{$q}%";$params[]="%{$q}%";$params[]="%{$q}%";}if($actionFilter!==''){$where[]='a.action=?';$params[]=$actionFilter;}
$count=db()->prepare('SELECT COUNT(*) FROM audit_logs a LEFT JOIN users u ON u.id=a.user_id WHERE '.implode(' AND ',$where));$count->execute($params);$p=pagination((int)$count->fetchColumn(),30);$stmt=db()->prepare("SELECT a.*,u.full_name FROM audit_logs a LEFT JOIN users u ON u.id=a.user_id WHERE ".implode(' AND ',$where)." ORDER BY a.created_at DESC LIMIT {$p['per_page']} OFFSET {$p['offset']}");$stmt->execute($params);$logs=$stmt->fetchAll();
if(($_GET['export']??'')==='audit_csv'){$rows=[];foreach($logs as $l)$rows[]=[$l['created_at'],$l['full_name']?:'Public/System',$l['action'],$l['details'],$l['ip_address']];audit((int)$user['id'],'audit_csv_export','Exported current audit-log page');csv_download('audit-log-'.date('Y-m-d').'.csv',['Date','User','Action','Details','IP'],$rows);}
$modelFile=__DIR__.'/../ml/models/svm_sentiment.joblib';$metricsFile=__DIR__.'/../ml/models/model_metrics.json';$metrics=is_file($metricsFile)?json_decode((string)file_get_contents($metricsFile),true):null;$health=predict_sentiment('Mabilis at maayos ang serbisyo.');$counts=['active_offices'=>(int)db()->query("SELECT COUNT(*) FROM offices WHERE status='active'")->fetchColumn(),'users'=>(int)db()->query('SELECT COUNT(*) FROM users')->fetchColumn(),'feedback'=>(int)db()->query('SELECT COUNT(*) FROM feedback WHERE is_void=0')->fetchColumn(),'reviews'=>(int)db()->query("SELECT COUNT(*) FROM feedback WHERE review_status='needs_review' AND is_void=0")->fetchColumn(),'training'=>(int)db()->query('SELECT COUNT(*) FROM training_candidates')->fetchColumn(),'locked'=>(int)db()->query('SELECT COUNT(*) FROM users WHERE locked_until>NOW()')->fetchColumn()];$actions=db()->query('SELECT DISTINCT action FROM audit_logs ORDER BY action')->fetchAll(PDO::FETCH_COLUMN);
render_dashboard_start('System & Audit','system');page_header('System Health & Audit','Verify the live SVM bridge, security state, backups, and protected activity.','<a class="btn" href="'.e(app_url('admin/backup.php')).'">Download SQL Backup</a>');
?>
<div class="stats-grid"><div class="stat-card"><small>Database</small><strong>Connected</strong><span>pasig_feedback_portal</span></div><div class="stat-card"><small>SVM Bridge</small><strong><?= $health['source']==='svm'?'Working':'Fallback' ?></strong><span><?= e($health['label']) ?> · <?= number_format((float)$health['confidence']*100,1) ?>%</span></div><div class="stat-card"><small>Active Offices</small><strong><?= $counts['active_offices'] ?></strong></div><div class="stat-card"><small>Review Queue</small><strong><?= $counts['reviews'] ?></strong></div><div class="stat-card"><small>Locked Accounts</small><strong><?= $counts['locked'] ?></strong></div></div>
<div class="content-grid"><section class="card"><div class="card-head"><div><h2>SVM Model Information</h2><p>Live path and development metrics</p></div></div><div class="formula-box"><strong>Python:</strong> <?= e(PYTHON_BIN) ?><br><strong>Script:</strong> <?= e(SVM_PREDICT_SCRIPT) ?><br><strong>Model:</strong> <?= is_file($modelFile)?'Ready':'Missing' ?><br><strong>Live source:</strong> <?= e($health['source']) ?><br><strong>Model version:</strong> <?= e(model_version()) ?></div><?php if($metrics):?><p><strong>Samples:</strong> <?= (int)$metrics['sample_count'] ?> · <strong>Holdout accuracy:</strong> <?= number_format((float)$metrics['accuracy']*100,2) ?>%</p><p class="help"><?= e($metrics['warning']??'') ?></p><?php endif;?></section><section class="card"><div class="card-head"><div><h2>Security & Governance</h2></div></div><div class="bar-list"><div>✓ Five-attempt lockout for <?= LOGIN_LOCK_MINUTES ?> minutes</div><div>✓ <?= SESSION_IDLE_TIMEOUT/60 ?>-minute inactivity timeout</div><div>✓ Forced change for newly created temporary passwords</div><div>✓ CSRF, prepared SQL, output escaping, audit logs</div><div>✓ Low-confidence manual review and preserved original labels</div><div>✓ SQL backup download; restore through phpMyAdmin</div><div>✓ <?= $counts['training'] ?> approved retraining candidate(s)</div></div></section></div>
<section class="card"><div class="card-head"><div><h2>Recent Audit Log</h2><p><?= (int)$p['total'] ?> protected event(s)</p></div><a class="btn secondary small" href="?<?= e(http_build_query(array_merge($_GET,['export'=>'audit_csv']))) ?>">Export Current Page</a></div><form class="filters compact" method="get"><input name="q" value="<?= e($q) ?>" placeholder="Search user, detail, IP"><select name="action_filter"><option value="">All actions</option><?php foreach($actions as $a):?><option value="<?= e($a) ?>" <?= $actionFilter===$a?'selected':'' ?>><?= e(status_label($a)) ?></option><?php endforeach;?></select><button class="btn">Apply</button></form><div class="table-wrap"><table><thead><tr><th>Date</th><th>User</th><th>Action</th><th>Details</th><th>IP</th></tr></thead><tbody><?php foreach($logs as $l):?><tr><td><?= e(date('M d, Y h:i A',strtotime($l['created_at']))) ?></td><td><?= e($l['full_name']?:'Public/System') ?></td><td><?= e(status_label($l['action'])) ?></td><td><?= e($l['details']) ?></td><td><?= e($l['ip_address']) ?></td></tr><?php endforeach;?><?php if(!$logs):?><tr><td colspan="5" class="empty-state">No audit records match the filters.</td></tr><?php endif;?></tbody></table></div><?= pagination_links($p) ?></section>
<?php render_dashboard_end(); ?>
